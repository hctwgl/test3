<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfUserSearchDao">
	<sql id="queryFields">
		id rid,gmt_create,user_id,keyword
	</sql>
	
	<insert id="addUserSearch" parameterType="AfUserSearchDo" useGeneratedKeys="true" keyProperty="rid">
		INSERT INTO af_user_search(gmt_create , user_id , keyword)
		VALUES (
    		NOW(),
    		#{userId},
    		#{keyword}
		)
	</insert>
	<select id = "getHistoryByUserId" parameterType="java.lang.Long" resultType="java.lang.String">
	 	SELECT DISTINCT(keyword) FROM af_user_search where is_delete = 0 and user_id = #{userId} ORDER BY gmt_create DESC LIMIT 10;
	</select>
	<update id="deleteHistory" parameterType="java.lang.Long">
		UPDATE  af_user_search SET is_delete = 1 ,gmt_create = NOW() WHERE user_id = #{userId} AND is_delete = 0;
	</update>
	
	
</mapper>