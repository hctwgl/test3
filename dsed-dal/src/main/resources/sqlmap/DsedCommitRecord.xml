<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.DsedCommitRecordDao">
	<sql id="queryFields">
		id, type, relate_id, content, url, commit_time, commit_num;
	</sql>
	
	<insert id="addRecord" parameterType="DsedCommitRecordDo" useGeneratedKeys="true" keyProperty="id">
		INSERT INTO af_commit_record(type, relate_id, content, url, commit_time, commit_num,gmt_create,gmt_modified)
		VALUES (
		#{type},
		#{relate_id},
		#{content},
		#{url},
		#{commit_time},
		#{commit_num},
		#{gmtCreate},
		#{gmtModified}
		)
	</insert>
	
	<select id="getRecordByTypeAndRelateId" parameterType="java.lang.String"  resultType="AfCommitRecordDo">
		SELECT 
			<include refid="queryFields"/>
		FROM 
			af_commit_record 
		WHERE
			relate_id = #{relate_id}
	</select>

</mapper>