<?xml version="1.0" encoding="UTF-8" standalone="no"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfPopupsDao">
	
	<sql id="activitySqlField">
		id,gmt_create,gmt_modified,creator,modifier,user_type,click_amount,name,img_url,url,type,is_delete,flag
	</sql>
	

	
	<select id="selectPopups" parameterType="java.lang.Long" resultType="AfPopupsDo">
		select <include refid="activitySqlField" /> from af_popups where is_delete = 0  and id = #{id}
	</select>
	
	<update id="updatePopups" parameterType="AfPopupsDo">
		UPDATE af_popups SET
		click_amount = click_amount + 1
		WHERE is_delete = 0 AND id = #{id ,jdbcType=BIGINT}

	</update>
	<update id="updatePopupsReachAmount" parameterType="java.lang.Long">
		UPDATE af_popups SET
		reach_amount = reach_amount + 1
		WHERE is_delete = 0 AND id = #{id ,jdbcType=BIGINT}
	</update>
	
</mapper>