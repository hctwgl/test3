<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ald.fanbei.api.dal.dao.AfBorrowExtendDao">

	<sql id="queryFields">
		id,in_bill,gmt_modified,gmt_create
	</sql>

	<insert id="addBorrowExtend" parameterType="AfBorrowExtendDo"  keyProperty="id">
		INSERT INTO af_borrow_extend(id,in_bill , gmt_modified , gmt_create)
		VALUES (
		#{id},
		#{inBill},
		NOW(),
		NOW()
		)
	</insert>
	<update id="updateBorrowExtend" parameterType="AfBorrowExtendDo" >
		UPDATE af_borrow_extend SET gmt_modified = NOW()
		<if test="inBill != null">
			,in_bill=#{inBill}
		</if>
		WHERE id = #{id}
	</update>


	<select id="getAfBorrowExtendDoByBorrowId" resultType="AfBorrowExtendDo">
		select <include refid="queryFields" /> from af_borrow_extend
		where id =#{borrowId}
	</select>

</mapper>