<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfBorrowLogDao">
	<sql id="queryAllFields">
		id rid,gmt_create,creator,type,borrow_id,user_id
	</sql>
	
	<insert id="addBorrowLog" parameterType="AfBorrowLogDo" useGeneratedKeys="true" keyProperty="rid">
		INSERT INTO af_borrow_log(gmt_create , creator , type,borrow_id,user_id)
		VALUES (
    		NOW(),
    		#{creator},
    		#{type},
    		#{borrowId},
    		#{userId}
		)
	</insert>
	
	<update id="updateBorrowLog" parameterType="AfBorrowLogDo" >
		UPDATE af_borrow_log SET gmt_modified = NOW()
			<if test="isDelete != null">
    			,is_delete=#{isDelete}
			</if>
			<if test="creator != null">
    			,creator=#{creator}
			</if>
			<if test="borrowId != null">
    			,borrow_id=#{borrowId}
			</if>
			<if test="userId != null">
    			,user_id=#{userId}
			</if>
			<if test="type != null">
    			,type=#{type}
			</if>
		WHERE id = #{rid}
	</update>
</mapper>
