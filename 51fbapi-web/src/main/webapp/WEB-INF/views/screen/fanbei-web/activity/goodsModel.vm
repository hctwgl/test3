<!DOCTYPE html>
<html lang="en">
<head>
    #set($version="2017052302")
    #set($src="http://f.51fanbei.com/h5/app/activity/07")
    <title>南极“冷空气”来袭</title>
</head>
<style>
    .wrap{
        padding:.18rem 0;
        background: white;
        margin-top: .01rem;
        overflow: hidden;
        width: 7.5rem;
        height: 3.3rem;
    }
    .coupon{
        margin:0 0 .08rem .08rem;
        border-radius:.08rem;
        float: left;
        width:2.38rem;
        height:1.61rem;
        overflow: hidden;
        background: url("$src/newf.png") no-repeat;
        background-size: cover;
        position: relative;
    }
    .coupon p{
        text-align: center;
        color:white;
        white-space: nowrap;
        position:absolute;
        width: 100%;
    }
    .recommend{
        width:100%;
        height:1.2rem;
        background:#fff;
        line-height:1.2rem;
        font-size: .32rem;
        text-align: center;
        margin:.2rem 0 .03rem 0;
    }
    .productList li{width:100%;height:2.92rem;background:#fff;margin-bottom: .03rem;}
    .productImg{width:2.92rem;height:2.92rem;float:left;}
    .productList li .productRight{width:3.6rem;float:left;padding:.55rem 0 0 6%;}
    .productList li p{display: inline-block;}
    .productList li .productRight .productDes{
        font-size: .3rem;
        height: 0.8rem;
        line-height: .38rem;
        overflow: hidden;
    }

    .productList li .productRight .buyPrice{height:.38rem;line-height: .38rem;
        font-size:.28rem;color:#999999;}
    .amount{
        height: 100%;
        position: relative;
        margin-top: 0.2rem;
    }
    .fan{
        position: absolute;
        top: 50%;
        left: 1.5rem;
        -webkit-transform: translateY(-50%);
        transform: translateY(-50%);
        width: .3rem;
        height: .3rem;
        background-image: url("$src/oppo4.png");
        -webkit-background-size: cover;
        background-size: cover;
    }
    .fanPrice{
        font-size:.26rem;
        color:#f94014;
        position: absolute;
        top: 50%;
        left: 1.9rem;
        -webkit-transform: translateY(-50%);
        transform: translateY(-50%);
    }
    .monthPrice{
        font-size:.28rem;
        color:#f94014;
        padding-top:.2rem;
    }
</style>
<body>
    <div id="vueCon">
        <img :src="tableContent.bannerUrl">
        <p class="recommend">热门推荐</p>
        <ul class="productList">
            <li v-for="item in tableContent.recommendGoodsList" @click="buyNow(item)">
                <div class="productImg"><img :src='item.goodsIcon'></div>
                <div class="productRight">
                    <p class="productDes">{{txtFix(item.goodName)}}</p>
                    <div class="monthPrice" v-if="item.goodsType==='1'">月供￥<span v-if="item.nperMap.isFree==1">{{item.nperMap.freeAmount}}</span><span v-else>{{item.nperMap.amount}}</span>起                    </div>
                    <div class="monthPrice" v-else style="opacity: 0">月供</div>
                    <div class="amount">
                        <p class="buyPrice">￥{{(item.saleAmount).toFixed(1)}}</p>
                        <i class="fan"></i>
                        <p class="fanPrice">￥{{item.rebateAmount}}</p>
                    </div>
                </div>
            </li>
        </ul>
    </div>
</body>
<script src="//cdn.bootcss.com/vue-resource/1.0.3/vue-resource.min.js"></script>
<script type="text/javascript" src="/dist/activity/07/newFriend.js"></script>
</html>
