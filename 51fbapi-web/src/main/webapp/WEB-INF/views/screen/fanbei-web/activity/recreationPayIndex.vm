<!DOCTYPE html>
<html lang="en">
<head>
    #set($version="201712061039")
    #set($src="http://f.51fanbei.com/h5/app/activity/11")
    <meta charset="UTF-8">
    <title>娱乐充值</title>
    <link rel="stylesheet" type="text/css" href="/dist/activity/11/gamePayIndex.css?version=$version">
</head>
<body>
<div id="recreationPayIndex" class="gamePayIndex">
    <div class="kong"></div>
    <div class="hotGame">
      <div class="title02"><p>热门推荐</p><p>免广告、看大片、全网最低</p></div>
        <div class="hotCont">
            <ul class="hotGameList clearFix">
                <li v-for="item in content.hotList" @click="hotGameClick(item)">
                    <img class="gameImg" :src="item.image">
                    <p class="gameName" style="-webkit-box-orient: vertical;">{{item.name}}</p>
                    <p class="fanMoney">返利{{fixNum((item.rebate)*100)}}%</p>
                </li>
            </ul>
        </div>
  </div>
    <ul class="gameRankList">
        <li v-for="items in content.groupList">
            <p class="rankTitle">{{items.groupName}}</p>
            <div class="everyCont clearFix" v-for="item in items.goodsList" @click="hotGameClick(item)">
                <img class="everyImg" :src="item.image">
                <p class="everyName">{{item.name}}</p>
                <p class="everyFan">返利{{fixNum((item.rebate)*100)}}%</p>
            </div>
        </li>
    </ul>
    <div class="kong"></div>
</div>
<script type="text/javascript" src="/dist/activity/11/recreationPayIndex.js?version=$version"></script>
</body>
</html>
