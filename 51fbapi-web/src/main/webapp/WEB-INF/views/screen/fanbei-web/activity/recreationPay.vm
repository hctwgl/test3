<!DOCTYPE html>
<html lang="en">
<head>
    #set($version="201711201338")
    #set($src="http://f.51fanbei.com/h5/app/activity/11")
    <meta charset="UTF-8">
    <title>充值服务</title>
    <link rel="stylesheet" type="text/css" href="/dist/activity/11/gamePay.css?version=$version">
</head>
<body>
<div id="recreationPay" class="gamePay">
    <div class="allTop">
        <div class="gameName">
            <p>服务名称</p>
            <span>{{initChooseFirst.gameName}}</span>
        </div>
        <div class="gamePass gameName">
            <p>{{initChooseFirst.accountType}}</p>
            <input type="text" placeholder=""/>
        </div>
        <div class="kong"></div>
        <div class="gameType">
            <p class="title">充值类型</p>
            <ul class="typeList clearFix">
                <li v-for="(item,index) in initChooseFirst.typeNameList" @click="gameTypeClick(index)">
                    <p class="typeName">{{item}}</p>
                </li>
            </ul>
        </div>
        <div class="gameMoney">
            <p class="title">面额</p>
            <ul class="moneyList clearFix">
                <li v-for="(item,index) in initChooseFirst.priceTypeList" @click="gameMoneyClick(index)">
                    <p class="typeName"><i class="goodsNum">{{item.chargeNum}}</i>{{initChooseFirst.chargeNumName}}</p>
                    <p class="typePrice"><i class="pricePay">{{fixNum((item.quantity)*(initChooseFirst.priceTimes)*discout)}}</i>元</p>
                </li>
            </ul>
        </div>
    </div>
    <div class="footer clearFix">
        <div class="left">
            <p class="payMoney">实付：<span>9.5元</span></p>
            <p class="fanMoney">充值成功后再返<span>0.5元</span></p>
        </div>
        <div class="sure" @click="sureClick()">确认充值</div>
    </div>
</div>
<script type="text/javascript" src="/dist/activity/11/recreationPay.js?version=$version"></script>
</body>
</html>
