<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfContactsOldDao">
	<sql id="queryField">
		id, uid, mobileBook, createTime		
	</sql>

	<select id="getAfContactsByUserId" resultType="AfContactsOldDo" >
		SELECT 
			<include refid="queryField" />
		FROM 
			af_contacts_old
		WHERE
			uid = #{userId}	 limit 1
	</select>
	
</mapper>