<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfBorrowDao">
	<sql id="queryFields">
		id rid,gmt_create, gmt_modified,borrow_no,user_id,type,name,amount,status,remark,nper,nper_repayment,overdue_num,order_id,
		goods_id,open_id,bank_id
	</sql>
	
	<select id="getBorrowById" resultType="AfBorrowDo">
		SELECT 
			<include refid="queryFields"/>
		FROM af_borrow WHERE is_delete = 0 AND id = #{id}
    </select>

	<insert id="addBorrow" keyProperty="rid" useGeneratedKeys="true" parameterType="AfBorrowDo">
		INSERT INTO af_borrow (
			gmt_create,
			gmt_modified,
			user_id,
			type,
			borrow_no,
			name,
			amount,
			status,
			nper,
			<if test="goodsId != null">
		    	goods_id, 
			</if>
			<if test="openId != null">
		    	open_id, 
			</if>
			bank_id
			)
			VALUES
			(
			#{gmtCreate},
			#{gmtCreate},
			#{userId},
			#{type},
			#{borrowNo},
			#{name},
			#{amount},
			#{status},
			#{nper},
			<if test="goodsId != null">
		    	#{goodsId},
			</if>
			<if test="openId != null">
		    	#{openId}, 
			</if>
			#{bankId}
			)
	</insert>
	
	<insert id="addBorrowBill" parameterType="java.util.List">
		  INSERT INTO af_borrow_bill (
		  gmt_create,
		  gmt_modified,
		  user_id,
		  borrow_id,
		  borrow_no,
		  name,
		  status,
		  gmt_borrow,
		  bill_year,
		  bill_month,
		  nper,
		  bill_nper,
		  bill_amount,
		  principle_amount,
		  interest_amount,
		  poundage_amount
		) 
		VALUES
	    <foreach collection = "billList" separator = "," item = "item" index = "index" > 
	    (
	    	NOW(),
	    	NOW(),
	  		#{item.userId}, 
	  		#{item.borrowId},
	  		#{item.borrowNo},  
	  		#{item.name}, 
	  		#{item.status},
	  		#{item.gmtBorrow}, 
	  		#{item.billYear},
	  		#{item.billMonth},
	  		#{item.nper}, 
	  		#{item.billNper}, 
	  		#{item.billAmount}, 
	  		#{item.principleAmount},
	  		#{item.interestAmount},
	  		#{item.poundageAmount}
	  	)
	  	</foreach> 
   </insert>
   
   <update id="updateBorrow">
		UPDATE
			af_borrow
		SET
			gmt_modified = NOW(),
			total_interest = #{totalInterest},
			total_poundage = #{totalPoundage}
		WHERE
			id = #{id}
	</update>
	
	<select id="getCurrentLastBorrowNo" resultType="java.lang.String">
		select borrow_no from af_borrow WHERE gmt_create=#{currentDate} ORDER BY id DESC LIMIT 1;
	</select>
</mapper>