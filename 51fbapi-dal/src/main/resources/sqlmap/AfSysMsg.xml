<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfSysMsgDao">
	<sql id="queryFields">
		id rid,gmt_create,user_id,title,content
	</sql>

	<insert id="addSysMsg" keyProperty="id" useGeneratedKeys="true" parameterType="AfSysMsgDo">
		INSERT INTO af_sys_msg (
			gmt_create,
			user_id,
			title,
			content
			)
			VALUES
			(
			NOW(),
			#{userId},
			#{title},
			#{content}
			)
	</insert>
	
	<select id="getSysMsgList" resultType="AfSysMsgDo">
		SELECT
			<include refid="queryFields"/>
		FROM
			af_sys_msg
		WHERE
			is_delete=0 AND user_id=#{userId}
		order by gmt_create desc
	</select>
</mapper>