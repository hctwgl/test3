<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfOrderTempDao">
	<sql id="queryFields">
		id rid,user_id,order_no,status,order_id
	</sql>
	
	<insert id="addUserOrder" parameterType="AfOrderTempDo" useGeneratedKeys="true" keyProperty="rid">
		INSERT INTO af_order_temp(user_id ,gmt_create,
		 <if test="orderId != null">
		    	order_id, 
			</if>order_no)
		VALUES (
    		#{userId},
    		NOW()
    		,<if test="orderId != null">
		    	#{orderId}, 
			</if>
    		#{orderNo}
		)
	</insert>
	
	<select id="getByOrderNo" resultType="AfOrderTempDo">
	    SELECT
	    	<include refid="queryFields" />
	    FROM
	   		 af_order_temp
	    WHERE
	    	is_delete = 0
	     AND
	         order_no =#{orderNo}
	</select>
	
	
	<update id="updateUserOrderTemp" parameterType="AfOrderTempDo" >
		update af_order_temp  set gmt_modified=NOW()
		<if test="status != null">
		    ,status=#{status}
		</if>
		<if test="userId != null">
		    ,user_id=#{userId}
		</if>
		where is_delete=0 and id = #{rid}
	</update>
</mapper>