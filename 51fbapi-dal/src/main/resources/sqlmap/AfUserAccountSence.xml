<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<!-- af_user_account_sence表 -->
<mapper namespace="com.ald.fanbei.api.dal.dao.AfUserAccountSenceDao">
    
    <!--基本的sql查询字段 公共引用...-->
    <sql id="queryFields">
        id rid,gmt_create,gmt_modified,scene,user_id,au_amount,used_amount,freeze_amount 
    </sql>
    
    <!-- 基本的sql查询条件公共引用 -->
    <sql id="commonCondition">
    	WHERE is_delete = 0
        <if test="rid !=null">
            AND id  = #{rid,jdbcType=INTEGER}
        </if>
        <if test="gmtCreate !=null">
            AND gmt_create = #{gmtCreate,jdbcType=TIMESTAMP}
        </if>
        <if test="gmtModified !=null">
            AND gmt_modified = #{gmtModified,jdbcType=TIMESTAMP}
        </if>
        <if test="scene != null">
            AND scene = #{scene,jdbcType=VARCHAR}
        </if>
        <if test="userId != null">
            AND user_id = #{userId,jdbcType=INTEGER}
        </if>
        <if test="auAmount != null">
            AND au_amount = #{auAmount,jdbcType=DECIMAL}
        </if>
        <if test="usedAmount != null">
            AND used_amount = #{usedAmount,jdbcType=DECIMAL}
        </if>
        <if test="freezeAmount != null">
            AND freeze_amount = #{freezeAmount,jdbcType=DECIMAL}
        </if>
    </sql>

    
    <insert id="saveRecord" parameterType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo" keyProperty="rid">
    	<selectKey resultType="java.lang.Long" keyProperty="rid" order="AFTER" >
	      SELECT LAST_INSERT_ID()
	    </selectKey>
    
        INSERT INTO af_user_account_sence
        <trim prefix="(" suffix=")" suffixOverrides="," >
	        <if test="gmtCreate != null">        
	            gmt_create,
	        </if>
	        <if test="gmtModified != null">        
	            gmt_modified,
	        </if>
	        <if test="scene != null">        
	            scene,
	        </if>
	        <if test="userId != null">        
	            user_id,
	        </if>
	        <if test="auAmount != null">        
	            au_amount,
	        </if>
	        <if test="usedAmount != null">        
	            used_amount,
	        </if>
	        <if test="freezeAmount != null">        
	            freeze_amount,
	        </if>
        </trim>
        
        <trim prefix="values (" suffix=")" suffixOverrides="," >
		    <if test="gmtCreate != null" >
		       #{gmtCreate,jdbcType=TIMESTAMP},
		    </if>
		    <if test="gmtModified != null" >
		       #{gmtModified,jdbcType=TIMESTAMP},
		    </if>
		    <if test="scene != null" >
		       #{scene,jdbcType=VARCHAR},
		    </if>
		    <if test="userId != null" >
		       #{userId,jdbcType=INTEGER},
		    </if>
		    <if test="auAmount != null" >
		       #{auAmount,jdbcType=DECIMAL},
		    </if>
		    <if test="usedAmount != null" >
		       #{usedAmount,jdbcType=DECIMAL},
		    </if>
		    <if test="freezeAmount != null" >
		       #{freezeAmount,jdbcType=DECIMAL},
		    </if>
        </trim>
    </insert>
    
    <update id="updateById"  parameterType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        UPDATE af_user_account_sence
          <set>
            gmt_modified = NOW(),
            <if test="gmtCreate != null">        
                gmt_create = #{gmtCreate,jdbcType=TIMESTAMP},                
            </if>
            <if test="scene != null">        
                scene = #{scene,jdbcType=VARCHAR},                
            </if>
            <if test="userId != null">        
                user_id = #{userId,jdbcType=INTEGER},                
            </if>
            <if test="auAmount != null">        
                au_amount = #{auAmount,jdbcType=DECIMAL},                
            </if>
            <if test="usedAmount != null">        
                used_amount = #{usedAmount,jdbcType=DECIMAL},                
            </if>
            <if test="freezeAmount != null">        
                freeze_amount = #{freezeAmount,jdbcType=DECIMAL}                
            </if>
        </set>    
        WHERE is_delete = 0 AND id = #{rid ,jdbcType=BIGINT}
    </update>

	<update id="updateUserSceneAuAmount"  parameterType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        INSERT INTO af_user_account_sence (scene,user_id,au_amount) 
        VALUES (#{scene,jdbcType=VARCHAR},#{userId,jdbcType=INTEGER},#{auAmount,jdbcType=DECIMAL})  
        ON DUPLICATE KEY UPDATE au_amount=#{auAmount,jdbcType=DECIMAL},gmt_modified = NOW(); 
    </update>

    <update id="updateFreezeAmount">
        UPDATE
        af_user_account_sence
        SET
        gmt_modified = NOW(),
        freeze_amount = freeze_amount + #{freezeAmount}
        WHERE
        user_id = #{userId} AND scene = #{scene}
    </update>

    <update id="updateUsedAmount">
        UPDATE
        af_user_account_sence
        SET
        gmt_modified = NOW(),
        used_amount = used_amount + #{usedAmount}
        WHERE
        user_id = #{userId} AND scene = #{scene}
    </update>

    <update id ="updateTrainInitUsedAmount">
    	UPDATE af_user_account_sence s
		LEFT JOIN (SELECT a.`user_id`, SUM(a.principle_amount) principle_amount
		FROM af_borrow_bill as a
		INNER JOIN af_borrow as b ON a.borrow_id=b.id
		INNER JOIN af_order as c ON b.order_id=c.id
		INNER JOIN af_trade_order as d ON c.id=d.order_id
		INNER JOIN af_trade_business_info as e ON d.business_id=e.business_id
		INNER JOIN af_business_type as f ON e.type=f.id
		WHERE a.is_delete=0 AND a.STATUS IN ( 'N','D')
		AND c.order_type = 'TRADE' AND f.`code` = 'TRAIN' AND a.`user_id` = #{userId}
		GROUP BY a.`user_id`) t
		on s.user_id = t.user_id and s.scene = 'TRAIN'
		set s.used_amount = IFNULL(t.principle_amount,0),
		s.gmt_modified = NOW()
		where s.scene = 'TRAIN' AND s.`user_id` = #{userId};
    </update>
    <update id ="updateOnlineInitUsedAmountByBills">
    	UPDATE af_user_account_sence s
		LEFT JOIN (SELECT a.`user_id`, SUM(a.principle_amount) principle_amount
		FROM af_borrow_bill as a
		INNER JOIN af_borrow as b ON a.borrow_id=b.id
		INNER JOIN af_order as c ON b.order_id=c.id
		WHERE a.is_delete=0 AND a.STATUS IN ( 'N','D')
		AND c.order_type <![CDATA[<>]]> 'TRADE' AND a.`user_id` = #{userId}
		GROUP BY a.`user_id`) t
		on s.user_id = t.user_id
		set s.used_amount = IFNULL(t.principle_amount,0),
		s.gmt_modified = NOW()
		where s.scene = 'ONLINE' AND s.`user_id` = #{userId};
    </update>
	<update id ="updateOnlineInitUsedAmountByOrderAp">
    	UPDATE af_user_account_sence s
		LEFT JOIN (SELECT c.`user_id`, SUM(c.actual_amount) actual_amount
		from af_order c
		WHERE c.order_type = 'SELFSUPPORT' AND pay_type = 'AP'
		AND (`STATUS`='PAID' or `STATUS`='DELIVERED') and c.gmt_create >= '2018-01-05' and c.user_id = #{userId}
		GROUP BY c.`user_id`) t
		on s.user_id = t.user_id
		set s.used_amount = s.used_amount + IFNULL(t.actual_amount,0),
		s.gmt_modified = NOW()
		where s.scene = 'ONLINE' AND s.`user_id` = #{userId};
    </update>
    <update id ="updateOnlineInitUsedAmountByOrderCp">
    	UPDATE af_user_account_sence s
		LEFT JOIN (SELECT c.`user_id`, SUM(c.borrow_amount) borrow_amount
		from af_order c
		WHERE c.order_type = 'SELFSUPPORT' AND pay_type = 'CP'
		AND (`STATUS`='PAID' or `STATUS`='DELIVERED') and c.gmt_create >= '2018-01-05' and c.user_id = #{userId}
		GROUP BY c.`user_id`) t
		on s.user_id = t.user_id
		set s.used_amount = s.used_amount + IFNULL(t.borrow_amount,0),
		s.gmt_modified = NOW()
		where s.scene = 'ONLINE' AND s.`user_id` = #{userId};
    </update>

	<select id="getByUserIdAndScene" resultType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        SELECT
        <include refid="queryFields" />
        FROM af_user_account_sence
        WHERE is_delete = 0 AND user_id = #{userId,jdbcType=INTEGER} and scene = #{scene,jdbcType=VARCHAR}
    </select>

    <select id="getByUserId" resultType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        SELECT
        <include refid="queryFields" />
        FROM af_user_account_sence
        WHERE is_delete = 0 AND user_id = #{userId}
    </select>

	<select id="getById" resultType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        SELECT
        <include refid="queryFields" />
        FROM af_user_account_sence
        WHERE is_delete = 0 AND id=#{rid ,jdbcType=BIGINT}
        LIMIT 0,1
    </select>

    <select id="getByUserIdAndType" resultType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        SELECT
        <include refid="queryFields" />
        FROM af_user_account_sence
        WHERE is_delete = 0 AND user_id=#{userId} AND scene=#{scene}
    </select>

    <select id="getBusinessTypeByOrderId" resultType="java.lang.String">
        SELECT d.`code`  FROM `af_order` as a
        INNER JOIN `af_trade_order` as b ON a.id = b.`order_id`
        INNER JOIN `af_trade_business_info` as c ON b.`business_id` = c.`business_id` INNER JOIN `af_business_type` as d ON c.`type` = d.`id`
        WHERE a.order_id = #{orderId}
    </select>

	<select id="getByCommonCondition" resultType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo" parameterType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        SELECT
        <include refid="queryFields" />
        FROM af_user_account_sence
        <include refid="commonCondition"/> 
        limit 0,1
    </select>
               
    <select id="getListByCommonCondition" resultType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo" parameterType="com.ald.fanbei.api.dal.domain.AfUserAccountSenceDo">
        SELECT
        <include refid="queryFields" />
        FROM af_user_account_sence
        <include refid="commonCondition"/>
    </select>
    
    <update id="updateUserSceneAuAmountByScene">
    	UPDATE
        af_user_account_sence
        SET
        gmt_modified = NOW(),
        au_amount = #{auAmount}
        WHERE
        user_id = #{userId} AND scene = #{scene}
    </update>
</mapper>
