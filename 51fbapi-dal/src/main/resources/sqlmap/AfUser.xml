<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.dal.dao.AfUserDao">
	<sql id="queryFields">
		id rid,birthday,gmt_modified,password,user_name,openid,unionid,is_subscribe,gmt_create,nick,avata,name,gender,error_count,mobile,salt,
		undisturbed_start_time,undisturbed_end_time,is_undisturbed,invite_code,invitor
	</sql>

	<sql id="queryCondition">
		<if  test="userId != null">
		    AND id=#{userId}
		</if>
		<if test="userName != null">
		    AND user_name=#{userName}
		</if>
		<if  test="nick != null">
		    AND nick=#{nick}
		</if>
		<if  test="mobile != null">
		    AND mobile=#{mobile}
		</if>
	</sql>


</mapper>