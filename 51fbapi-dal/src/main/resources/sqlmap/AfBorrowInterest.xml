<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfBorrowInterestDao">
	<sql id="queryFields">
		id rid,gmt_create,creator,borrow_id,interest,principle_amount
	</sql>
	
	<insert id="addBorrowInterest" parameterType="AfBorrowInterestDo" useGeneratedKeys="true" keyProperty="rid">
		INSERT INTO af_borrow_interest(gmt_create , creator , bill_id , interest,principle_amount)
		VALUES (
    		NOW(),
    		#{creator},
    		#{billId},
    		#{interest},
    		#{principleAmount}
		)
	</insert>
	
	<select id="getBorrowInterestCountByBorrowId" parameterType="java.lang.Long" resultType="java.lang.Integer">
		select count(1) from af_borrow_interest where is_delete=0 and bill_id=#{billId}
	</select>
	
</mapper>