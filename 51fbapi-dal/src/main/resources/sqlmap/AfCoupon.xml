<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfCouponDao">
	<sql id="queryAllFields">
		id rid,gmt_create,gmt_modified,gmt_start,gmt_end,name,status,limit_amount,amount,use_rule,valid_days,type,limit_count
	</sql>
	
	<select id="selectCouponByCouponIds" resultType="AfCouponDo">
	    SELECT
	    	<include refid="queryAllFields" />
	    FROM
	   		 af_coupon
	    WHERE
	    	is_delete = 0
	    AND
			 id in (${ids})
	</select>
	
	<select id="getCouponById" resultType="AfCouponDo">
	    SELECT
	    	<include refid="queryAllFields" />
	    FROM
	   		 af_coupon
	    WHERE
	    	is_delete = 0
	    AND
			id = #{couponId}
	</select>

</mapper>
