<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfInterimDetailDao">
    <sql id="queryFields">
        id rid,user_id,gmt_create,gmt_modify,order_id,amount,interim_used,type
    </sql>

    <insert id="addAfInterimDetail" parameterType="AfInterimDetailDo" useGeneratedKeys="true" keyProperty="rid">
        INSERT INTO af_interim_detail(user_id,gmt_create,gmt_modify,order_id,amount,interim_used,type)
        VALUES (
        #{userId},
        NOW(),
        NOW(),
        #{orderId},
        #{amount},
        #{interimUsed},
        #{type}
        )
    </insert>
</mapper>