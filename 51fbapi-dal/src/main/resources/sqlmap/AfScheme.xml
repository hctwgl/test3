<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfSchemeDao">
	<sql id="queryFields">
		id rid,gmt_create,gmt_modified,name,is_open,interest_free_id,tag
	</sql>
	
	<select id="getSchemeById" resultType="AfSchemeDo">
		SELECT
			<include refid="queryFields" />
		FROM
			af_scheme
		WHERE
			is_delete =0
		AND
			id = #{id}
	</select>

</mapper>