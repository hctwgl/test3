<?xml version="1.0" encoding="UTF-8" ?>  
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ald.fanbei.api.dal.dao.AfBankDao">
	<sql id="queryFields">
		id rid,gmt_create,bank_code,bank_name,bank_icon
	</sql>
	
	<insert id="addBank" parameterType="AfBankDo" useGeneratedKeys="true" keyProperty="rid">
		INSERT INTO af_bank(gmt_create , bank_code , bank_name , bank_icon)
		VALUES (
    		NOW(),
    		#{bankCode},
    		#{bankName},
    		#{bankIcon}
		)
	</insert>
	
	<select id="getBankList" resultType="AfBankDo">
		SELECT 
			<include refid="queryFields"/>
		FROM af_bank WHERE is_delete = 0 AND is_valid = 'Y'
	</select>
	
	<update id="updateBank" parameterType="AfBankDo" >
		UPDATE af_bank SET gmt_modified = NOW()
			<if test="isDelete != null">
    			,is_delete=#{isDelete}
			</if>
			<if test="bankCode != null">
    			,bank_code=#{bankCode}
			</if>
			<if test="bankName != null">
    			,bank_name=#{bankName}
			</if>
			<if test="bankIcon != null">
    			,bank_icon=#{bankIcon}
			</if>
		WHERE id = #{rid}
	</update>
</mapper>